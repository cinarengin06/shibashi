import type{FirstJourneyPlan,OnboardingProfile}from'./types';

export function deriveOnboardingShen(profile:OnboardingProfile):FirstJourneyPlan['recommendedShen']{const intentionMap={calm:'po',energy:'hun',focus:'yi',resilience:'zhi',connection:'shen'}as const;const bodyMap={'breath-shoulders':'po','hips-sides':'hun','center-balance':'yi','back-knees-feet':'zhi','whole-body':'shen'}as const;const scores={po:0,hun:0,yi:0,zhi:0,shen:0};scores[intentionMap[profile.practiceIntention]]+=3;scores[bodyMap[profile.bodyFocus]]+=2;if(profile.experienceLevel==='none')scores.shen+=1;if(profile.experienceLevel==='regular')scores.hun+=1;return(Object.entries(scores).sort((a,b)=>b[1]-a[1])[0]?.[0]??intentionMap[profile.practiceIntention])as FirstJourneyPlan['recommendedShen']}

export function createFirstJourneyPlan(profile:OnboardingProfile):FirstJourneyPlan{const experienced=profile.experienceLevel==='regular',movementCount=experienced?4:profile.experienceLevel==='some'?3:2,dailyMinutes=experienced?14:profile.experienceLevel==='some'?10:7;const focusByBody={'breath-shoulders':'nefes ve omuz rahatlığı','hips-sides':'akıcı dönüş ve yan beden','center-balance':'merkez ve denge','back-knees-feet':'güvenli köklenme','whole-body':'bütün beden farkındalığı'}as const;const intentionFocus={calm:'sakinleşme',energy:'yenilenme',focus:'dikkat',resilience:'devam gücü',connection:'bedenle bağ'}as const;const focuses=[focusByBody[profile.bodyFocus],intentionFocus[profile.practiceIntention],'nefes-hareket uyumu'];const recommendedShen=deriveOnboardingShen(profile);return{dailyMinutes,movementIds:Array.from({length:movementCount},(_,index)=>`movement-${index+1}`),breathingPracticeId:'deep-natural-breath',awarenessPracticeId:'center-awareness',reassessmentDay:7,reasonSummary:`${focuses[0]} ve ${focuses[1]} odağında, ${dailyMinutes} dakikalık ulaşılabilir bir başlangıç hazırladık.`,focuses,recommendedShen}}
